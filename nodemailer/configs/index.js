const nodemailerSiteConfig = require("./nodemailer-site-config");
const nodemailerGmailConfig = require("./nodemailer-gmail-config");

module.exports = {
    nodemailerSiteConfig,
    nodemailerGmailConfig
}